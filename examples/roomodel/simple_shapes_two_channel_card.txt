# Two-channel shape-based example for roomodel using simple_shapes_two_channel.root.
#
# Step 1: generate the shapes workspace in examples/roomodel:
#   python simple_shapes_two_channel.py
# Step 2: build the model from this card:
#   pymodel roomodel build examples/roomodel/simple_shapes_two_channel_card.txt model.root
# Step 3: run analysis:
#   pymodel roomodel analyze --model-file model.root --toys 50

shapes * barrel simple_shapes_two_channel.root
shapes * endcap simple_shapes_two_channel.root
shapes data_obs barrel simple_shapes_two_channel.root
shapes data_obs endcap simple_shapes_two_channel.root

bin     barrel  barrel  endcap  endcap
process  sig     bkg      sig     bkg
process  -1       1       -1       1
rate     12      80        6      200
