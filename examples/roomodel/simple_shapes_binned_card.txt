# Binned example model card for the roomodel backend.
#
# Signal: Gaussian(mu=105, sigma=0.35), yield=12
# Background: Exponential(lam=-0.25), yield=80
# Observable: mass in [100, 110], 20 uniform bins
# Observed counts are identical to the zmodel and hfmodel binned examples (total=90).
#
# Step 1: generate the shapes workspace:
#   cd examples/roomodel && python simple_shapes_binned.py
# Step 2: build the model:
#   pymodel roomodel build simple_shapes_binned_card.txt model_binned.root
# Step 3: run the analysis:
#   pymodel roomodel analyze -m model_binned.root

shapes    *        * simple_shapes_binned.root
shapes data_obs    * simple_shapes_binned.root

observation demo 90

bin                  demo   demo
process               sig    bkg
process               -1      1
rate                  12     80

# Rate-only uncertainties (no shape systematics)
lumi      lnN  1.05  1.05
bkg_norm  lnN   -    1.10
