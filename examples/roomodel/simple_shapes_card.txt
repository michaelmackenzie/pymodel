# Example model card using simple_shapes.root (generated by simple_shapes.py).
#
# Step 1: generate the shapes workspace in the examples/roomodel directory:
#   cd examples/roomodel && python simple_shapes.py
# Step 2: build the model bundle from this card:
#   pymodel roomodel build simple_shapes_card.txt model.root
# Step 3: run analysis:
#   pymodel roomodel analyze --model-file model.root --toys 50

shapes    *     * simple_shapes.root
shapes data_obs * simple_shapes.root

observation demo 106

bin                  demo   demo
process               sig   bkg
process               -1     1
rate                  12    800
		     
lumi           lnN   1.05   1.05
dioN           lnN    -     1.02
		     
sig_mu_offset param 0.0 1.0
